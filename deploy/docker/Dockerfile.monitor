# Build stage
FROM node:22-alpine AS builder

WORKDIR /app

# Copy package files
COPY package*.json ./
COPY apps/monitor-service/package*.json ./apps/monitor-service/
COPY packages/*/package*.json ./packages/

# Install dependencies
RUN npm install --frozen-lockfile

# Copy source code
COPY apps/monitor-service ./apps/monitor-service
COPY packages ./packages

# Build monitor service
WORKDIR /app/apps/monitor-service
RUN npm run build

# Production stage
FROM node:22-alpine

WORKDIR /app

# Install production dependencies only
COPY package*.json ./
COPY apps/monitor-service/package*.json ./apps/monitor-service/
COPY packages/db/package*.json ./packages/db/
COPY packages/logger/package*.json ./packages/logger/
COPY packages/common/package*.json ./packages/common/

RUN npm install --production --workspace=@openclaw/monitor-service

# Copy built files
COPY --from=builder /app/apps/monitor-service/dist ./apps/monitor-service/dist
COPY --from=builder /app/packages/db/dist ./packages/db/dist
COPY --from=builder /app/packages/logger/dist ./packages/logger/dist
COPY --from=builder /app/packages/common/dist ./packages/common/dist

# Create non-root user
RUN addgroup -g 1001 -S nodejs && \
    adduser -S nodejs -u 1001

USER nodejs

EXPOSE 3003

HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
  CMD node -e "require('http').get('http://localhost:3003/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"

CMD ["node", "apps/monitor-service/dist/index.js"]
